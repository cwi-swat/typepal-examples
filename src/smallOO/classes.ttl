test S1 [[
    class C {
        int i;
        int f (int b) = i + b;
    }
]]

test S2 [[
    class C {
        int i;
        str s;
        int f () = i + s;
    }
]] expect { "+ is not defined for `intType()` and `strType()`" }